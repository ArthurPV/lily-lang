fun add(x, y) = return x+y;

fun is_zero(x) = if x == 0 do true;;

type Letter: enum =
	A i32,
	B i32
end

type Person: record =
	name Str,
	age Uint8
end

type Persons: alias = i32;

object Work: trait =
	@get_work :: self -> Str
end

fun main =
	x := 20
	if x == 0 do
		true
	elif x == 1 do
		false
	else
		true
	end
end
