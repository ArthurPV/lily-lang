fun add(x, y) = x+y;
fun sub(x, y) = x-y;
fun mul(x, y) = x*y;

type Name: enum(error) =
	F
	G
end

type Person: record =
	name string
	age u32
end

fun div(x, y) = x/y;

type TT: alias = int;

object PersonObj: enum =
	F
	G
end

object Work: trait =
	@ff
end

object Person: class =
	@name string
	@age u32
end
